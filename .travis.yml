language: php

# php compatibility
php: ['7.3', '7.4', '8.0']

cache:
  - directories:
    - $HOME/.composer

before_script:
  - phpenv config-rm xdebug.ini || true
  - travis_retry composer upgrade --no-interaction --prefer-dist
  - travis_retry bash develop/install-development-tools

script:
  - tools/php-cs-fixer fix --verbose --dry-run
  - tools/phpcs --colors -sp
  - vendor/bin/phpunit --testsuite complete --testdox --no-interaction --verbose
  - tools/phpstan analyze --level max --no-progress --verbose src/ tests/

notifications:
  email:
    if: branch = master
